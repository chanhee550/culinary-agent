# 백엔드 전용 추가 의존성. 루트 requirements.txt와 함께 설치하세요:
#   pip install -r requirements.txt -r backend/requirements.txt
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9
# 음성 (ASR/TTS)
# ASR — OpenAI Whisper (github.com/openai/whisper, MIT). 로컬 실행, API 키 불필요.
#       torch를 함께 끌어오며 오디오 디코딩에 ffmpeg가 필요합니다.
openai-whisper>=20231117
# TTS — edge-tts (Microsoft Edge 무료 음성). API 키 불필요.
edge-tts>=7.0.0
# 인증
bcrypt>=4.0.0,<5.0.0
python-jose[cryptography]>=3.3.0,<4.0.0
google-auth>=2.30.0,<3.0.0
email-validator>=2.0.0,<3.0.0
# 모더레이션 (Hugging Face Inference API)
httpx>=0.27.0,<1.0.0
